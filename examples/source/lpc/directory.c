/**
 * Ensures that a directory exists by creating it and its parent directories if
 * necessary.
 *
 * @param {string} path - The path of the directory to ensure.
 * @return {int} - 1 if successful, 0 if failed.
 */
mixed assure_dir(string path) {}

/**
 * Returns the directory of the given object.
 *
 * @param {object} ob - The object to query (optional, default: previous object).
 * @return {string} - The directory path of the object.
 */
string query_directory(object ob) {}
